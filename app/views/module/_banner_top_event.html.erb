
 <% if !@blog_deal.nil? %>
		
	<div class="banner_item" id="zone_37">
				
		<a href="<%= url_for({ controller: "blog", action: "show", blog_id: @blog_deal.id})  %>" style="background-image: url(<%= asset_path(@blog_deal.image_url.banner_small_top_image) %>)" data-title="<%= @blog_deal.title %>" title="<%= @blog_deal.title %>"></a>
		
	</div>
	
<% end %>