<li class="<%=  self_or_other(message) %>">
  
  <div class="avatar">
    
    <img src="http://placehold.it/50x50" />
  
  </div>
  
  <div class="chatboxmessagecontent">
    
    <p><%= message.body %></p>
    
    <time datetime="<%= message.created_at %>" title="<%= message.created_at.strftime("%d %b %Y at %I:%M%p") %>">
      
      <%= message_interlocutor(message).first_name.to_s + " " + message_interlocutor(message).last_name.to_s %> • <%= message.created_at.in_time_zone("Asia/Bangkok").strftime("%H:%M %p") %>
    
    </time>
  
  </div>

</li>